<Page x:Class="MatStat.RegressionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MatStat"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegressionPage">

    <Grid VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Прогнозирование" HorizontalAlignment="Center"/>
            <DataGrid ItemsSource="{Binding MainTable}"
                      IsReadOnly="True"
                      ColumnWidth="*"
                      CanUserAddRows="False"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Результат прогноза" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding FutureText}"/>
        </StackPanel>
    </Grid>
</Page>
